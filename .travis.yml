language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mvcpress' ]; then cd .. && eval "mv $currentfolder generator-mvcpress" && cd generator-mvcpress; fi

